import React, { useState } from 'react';

import {View} from 'react-native'

export default function CreateScreen() {

    const [, set] = useState();

        return (

            <View />

        );
}