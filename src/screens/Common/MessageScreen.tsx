import React, { useState } from 'react';

import {View} from 'react-native'

export default function MessageScreen() {

    const [, set] = useState();

        return (

            <View />

        );
}